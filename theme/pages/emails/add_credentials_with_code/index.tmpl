{{ template "base_email" . }}

{{ define "content" }}
<div class="heading2">{{ if eq .Data.Details.AddCredentials.Type "webauthn" }}{{ T "emails.add_credentials_with_code.heading_passkey" }}{{ else }}{{ T "emails.add_credentials_with_code.heading_credential" }}{{ end }}</div>
<div class="main-text">
    {{ if eq .Data.Details.AddCredentials.Type "webauthn" }}{{ T "emails.add_credentials_with_code.body_passkey" }}{{ else }}{{ T "emails.add_credentials_with_code.body_credential" }}{{ end }}
</div>
<div class="main-text">
    {{ if eq .Data.Details.AddCredentials.Type "webauthn" }}{{ T "emails.add_credentials_with_code.instruction_passkey" }}{{ else }}{{ T "emails.add_credentials_with_code.instruction_credential" }}{{ end }}
</div>
<div class="code-container">{{ .Data.OTP.Code }}</div>

<div class="caption content-caption">
{{ T "emails.add_credentials_with_code.footer_warning" }}
</div>
{{ end }}