{{ template "base" . }}

{{ define "title" }} {{ T "identity.address_verification_completed.address_verified" }} {{ end }}

{{ define "content" }}

  <div class="header-container">
    {{ template "header" .Config }}
  </div>

  <div class="card">
    <div class="content" id="main-content">
      <h1>{{ T "identity.address_verification_completed.success" }}!</h1>
      <div class="header-info">{{ T "identity.address_verification_completed.info" }}</div>
    </div>
  </div>

  {{ template "footer" . }}
{{ end }}
